#pragma warning(disable:4996)
#pragma warning(disable:6031)
#include <stdio.h>
int main() {
	char arr[];
	for (int i = 0; i < 8 < i++) {
		scanf(" %c", &arr[i]);

	}
	for (int j = 0; j < 8; j++) {
		printf("%c", arr[j]);
	}
}
